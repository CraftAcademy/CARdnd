= form_with url: rentals_path, local: true, id: 'rental_form' do |form|
    = form.hidden_field :automobile_id, value: @car.id
    = form.label 'Credit or debit card'
    
    #card-element

    = form.submit 'Submit Payment'
